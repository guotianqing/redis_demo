#Program("cppredis_test", "cppredis_test.cpp", LIBS = ['cpp_redis', 'tacopie', 'pthread'], CCFLAGS = '-std=c++11')
#Program("hiredis_test", "hiredis_test.cpp", LIBS = ['hiredis', 'event', 'pthread'], CCFLAGS = '-std=c++11')
#Program("read_redis_to_csv", "read_redis_to_csv.cpp", LIBS = ['hiredis', 'event'], CCFLAGS = '-std=c++11')
Program("redis_timeusage", ["redis_timeusage.cpp", 'redis_mgr.cpp', '../cross_platform_utils/cross_platform_utils.cpp'], LIBS = ['cpp_redis', 'tacopie', 'pthread'], CCFLAGS = '-std=c++11')
